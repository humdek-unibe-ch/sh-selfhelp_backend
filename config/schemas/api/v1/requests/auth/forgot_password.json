{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Auth Forgot Password Request",
  "description": "Request a password-reset email for the given address. The response is always a generic success so the endpoint cannot be used to enumerate accounts.",
  "type": "object",
  "properties": {
    "email": {
      "type": "string",
      "format": "email",
      "description": "Email address of the account to recover."
    }
  },
  "required": [
    "email"
  ],
  "additionalProperties": false
}
