{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Auth Reset Password Request",
  "description": "Set a new password using the one-time recovery token from the reset email.",
  "type": "object",
  "properties": {
    "id_users": {
      "type": "integer",
      "minimum": 1,
      "description": "ID of the user resetting their password (from the reset link)."
    },
    "token": {
      "type": "string",
      "minLength": 1,
      "description": "One-time recovery token from the reset link."
    },
    "password": {
      "type": "string",
      "minLength": 8,
      "description": "The new password."
    }
  },
  "required": [
    "id_users",
    "token",
    "password"
  ],
  "additionalProperties": false
}
