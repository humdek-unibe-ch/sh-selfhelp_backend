{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Mobile Preview Session Exchange Request",
  "description": "Exchange a one-time mobile-preview code for a short-lived, scoped preview JWT. PUBLIC route: the one-time code IS the credential (single-use, short TTL, invalid after exchange). additionalProperties:false rejects any unknown field.",
  "type": "object",
  "properties": {
    "code": {
      "type": "string",
      "minLength": 16,
      "maxLength": 256,
      "description": "The opaque one-time preview code minted by the admin session endpoint."
    }
  },
  "required": ["code"],
  "additionalProperties": false
}
