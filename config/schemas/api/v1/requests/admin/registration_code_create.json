{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Create Registration Code Request",
  "description": "Schema for creating a new registration code.",
  "type": "object",
  "properties": {
    "code": {
      "type": "string",
      "minLength": 1,
      "maxLength": 16,
      "description": "The registration code value. Must be unique."
    },
    "id_groups": {
      "type": "integer",
      "minimum": 1,
      "description": "ID of the group to associate with this code."
    }
  },
  "required": ["code", "id_groups"],
  "additionalProperties": false
}
