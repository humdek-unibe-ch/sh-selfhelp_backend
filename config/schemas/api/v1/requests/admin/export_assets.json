{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Export Assets Request",
  "description": "Request schema for exporting assets as a ZIP bundle",
  "properties": {
    "folders": {
      "type": "array",
      "description": "Folders to include in the bundle. Empty or omitted exports every folder the caller may read.",
      "items": {
        "type": "string",
        "pattern": "^[a-zA-Z0-9_-]+$",
        "maxLength": 100
      }
    }
  },
  "additionalProperties": false
}
