{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Admin System Maintenance Set Request",
  "description": "Enable/disable maintenance mode for the CURRENT instance. Intentionally has NO instance_id: the backend derives + verifies the instance identity server-side. additionalProperties:false rejects any instance_id or other unknown field.",
  "type": "object",
  "properties": {
    "enabled": {
      "type": "boolean",
      "description": "true enables maintenance mode; false disables the file-backed state."
    },
    "message": {
      "type": "string",
      "maxLength": 500,
      "description": "Optional operator-facing note shown while in maintenance."
    }
  },
  "required": ["enabled"],
  "additionalProperties": false
}
