{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Plugin Install Request",
  "description": "Admin payload for requesting a plugin install. The manifest is validated separately against docs/plugins/plugin-manifest.schema.json by PluginManifestValidator.",
  "additionalProperties": false,
  "required": ["manifest"],
  "properties": {
    "manifest": {
      "type": "object",
      "description": "Full plugin.json contents. Validated against the plugin manifest schema by the host."
    },
    "registryEntry": {
      "type": ["object", "null"],
      "description": "Optional registry payload (checksums, signature, source) attached to the operation snapshot."
    }
  }
}
