{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Create Child Section Request",
  "description": "Request schema for creating one or more child sections inside another section",
  "oneOf": [
    {
      "type": "object",
      "properties": {
        "styleId": {
          "type": "integer",
          "description": "The ID of the style to use for the section."
        },
        "position": {
          "type": "integer",
          "description": "The position of the section within the parent section. If null, it will be added at the end."
        },
        "name": {
          "type": "string",
          "minLength": 1,
          "description": "The name of the section."
        }
      },
      "required": ["styleId", "position"],
      "additionalProperties": false
    },
    {
      "type": "array",
      "minItems": 1,
      "maxItems": 20,
      "items": {
        "type": "object",
        "properties": {
          "styleId": {
            "type": "integer",
            "description": "The ID of the style to use for the section."
          },
          "position": {
            "type": "integer",
            "description": "The position of the section within the parent section. If null, it will be added at the end."
          },
          "name": {
            "type": "string",
            "minLength": 1,
            "description": "The name of the section."
          }
        },
        "required": ["styleId", "position"],
        "additionalProperties": false
      }
    }
  ]
}
