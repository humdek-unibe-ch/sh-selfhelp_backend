{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Create Page Section Request",
  "description": "Request schema for creating one or more sections on a page",
  "oneOf": [
    {
      "type": "object",
      "properties": {
        "styleId": {
          "type": "integer",
          "description": "The ID of the style to use for the section."
        },
        "position": {
          "type": "integer",
          "description": "The position of the section within the page. If null, it will be added at the end."
        },
        "name": {
          "type": "string",
          "minLength": 1,
          "description": "The name of the section."
        }
      },
      "required": ["styleId", "position"],
      "additionalProperties": false
    },
    {
      "type": "array",
      "minItems": 1,
      "maxItems": 20,
      "items": {
        "type": "object",
        "properties": {
          "styleId": {
            "type": "integer",
            "description": "The ID of the style to use for the section."
          },
          "position": {
            "type": "integer",
            "description": "The position of the section within the page. If null, it will be added at the end."
          },
          "name": {
            "type": "string",
            "minLength": 1,
            "description": "The name of the section."
          }
        },
        "required": ["styleId", "position"],
        "additionalProperties": false
      }
    }
  ]
}
