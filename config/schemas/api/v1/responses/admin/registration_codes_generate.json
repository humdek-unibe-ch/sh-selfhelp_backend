{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Admin Registration Codes Generate Success Response",
    "description": "Schema for the successful response of generating registration codes for an admin.",
    "allOf": [
        { "$ref": "../../common/_response_envelope.json" }
    ],
    "properties": {
        "data": {
            "type": "object",
            "properties": {
                "codes": {
                    "type": "array",
                    "description": "The registration codes that were created by this request.",
                    "items": { "$ref": "../../entities/registrationCodeEntity.json" }
                }
            },
            "required": ["codes"]
        }
    },
    "required": ["data"]
}
