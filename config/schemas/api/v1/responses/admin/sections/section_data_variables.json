{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Admin Section Data Variables Response",
    "description": "Interpolation variable picker for a section: a token => human label map. The token is the value inserted as {{token}} (e.g. 'scope.field_name', 'global.var_name', 'system.user_name'); the label is the text shown in the CMS variable picker.",
    "allOf": [
        { "$ref": "../../common/_response_envelope.json" }
    ],
    "properties": {
        "data": {
            "type": "object",
            "properties": {
                "data_variables": {
                    "type": "object",
                    "description": "Map of interpolation token => display label.",
                    "additionalProperties": { "type": "string" }
                }
            },
            "required": ["data_variables"]
        }
    },
    "required": ["data"]
}
