{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "System Lookups Response Schema",
    "description": "Schema for the GET /lookups (route name `system_lookups`) endpoint response. Authenticated-only reference data (timezones, type codes, weekdays, audit categories) consumed by both admin tooling and public frontend styles such as ProfileStyle. Schema file path kept under admin/common/ for historical reasons; the route itself is no longer admin-gated since migration Version20260508160000.",
    "type": "object",
    "allOf": [
        {
            "$ref": "../../common/_response_envelope.json"
        }
    ],
    "properties": {
        "data": {
            "type": "array",
            "description": "Array of lookup entities",
            "items": {
                "$ref": "../../../entities/lookupEntity.json"
            }
        }
    },
    "required": ["data"]
}
