# config/packages/test/framework.yaml
framework:
    test: true
    cache:
        app: cache.adapter.filesystem
    session:
        storage_factory_id: session.storage.factory.mock_file
    profiler:
        # Disable the profiler in tests for performance, unless you are specifically testing it
        collect: false
