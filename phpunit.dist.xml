<?xml version="1.0" encoding="UTF-8"?>

<!-- https://phpunit.readthedocs.io/en/latest/configuration.html -->
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         colors="true"
         failOnDeprecation="true"
         failOnNotice="true"
         failOnWarning="true"
         defaultTestSuite="Project Test Suite"
         bootstrap="tests/bootstrap.php"
         cacheDirectory=".phpunit.cache"
>
    <php>
        <ini name="display_errors" value="1" />
        <ini name="error_reporting" value="-1" />
        <server name="APP_ENV" value="test" force="true" />
        <server name="SHELL_VERBOSITY" value="-1" />
        <server name="KERNEL_CLASS" value="App\Kernel" />

        <!-- ###+ symfony/mailer ### -->
        <env name="MAILER_DSN" value="null://null"/>
        <!-- ###- symfony/mailer ### -->
    </php>

    <!--
        Test suites map to the folder convention in plan §15.

        - Smoke        fast deploy/boot verification (tests/Smoke)
        - Unit         pure unit + DB-free plugin-host tests
        - Integration  kernel/HTTP tests that hit the test DB
        - Golden       end-to-end business-workflow tests (slow, @group golden)
        - unit         LEGACY alias kept for .github/workflows/plugin-host-check.yml
                       which selects the unit testsuite (DB-free plugin-host tests)

        The default "Project Test Suite" runs everything for `composer test`.
    -->
    <testsuites>
        <testsuite name="Project Test Suite">
            <directory>tests</directory>
        </testsuite>
        <testsuite name="Smoke">
            <directory>tests/Smoke</directory>
        </testsuite>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
            <directory>tests/Plugin</directory>
        </testsuite>
        <testsuite name="Integration">
            <directory>tests/Integration</directory>
            <directory>tests/Controller</directory>
            <directory>tests/Service</directory>
            <directory>tests/Api</directory>
        </testsuite>
        <testsuite name="Golden">
            <directory>tests/Golden</directory>
        </testsuite>
        <testsuite name="Certification">
            <directory>tests/Certification</directory>
        </testsuite>
        <testsuite name="unit">
            <directory>tests/Plugin</directory>
        </testsuite>
    </testsuites>

    <source ignoreSuppressionOfDeprecations="true"
            ignoreIndirectDeprecations="true"
            restrictNotices="true"
            restrictWarnings="true"
    >
        <include>
            <directory>src</directory>
        </include>

        <deprecationTrigger>
            <method>Doctrine\Deprecations\Deprecation::trigger</method>
            <method>Doctrine\Deprecations\Deprecation::delegateTriggerToBackend</method>
            <function>trigger_deprecation</function>
        </deprecationTrigger>
    </source>

    <extensions>
        <bootstrap class="Symfony\Bridge\PhpUnit\SymfonyExtension">
            <parameter name="clock-mock-namespaces" value="App" />
            <parameter name="dns-mock-namespaces" value="App" />
        </bootstrap>
        <!--
            DAMA wraps each test in a transaction rolled back on tearDown so
            the seeded QA baseline stays stable across the whole run. See
            config/packages/test/doctrine.yaml.
        -->
        <bootstrap class="DAMA\DoctrineTestBundle\PHPUnit\PHPUnitExtension" />
    </extensions>
</phpunit>
